local inst = mc.mcGetInstance()
if AuxLib and AuxLib.Request then
  AuxLib.Request(inst, { device = "boot", action = "auto.toggle" })
else
  mc.mcCntlSetLastError(inst, "Missing AuxLib.Request: boot auto.toggle")
end