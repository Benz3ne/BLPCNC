local inst = mc.mcGetInstance()
if AuxLib and AuxLib.Request then
  AuxLib.Request(inst, { device = "dust", action = "manual.toggle" })
else
  mc.mcCntlSetLastError(inst, "Missing AuxLib.Request: dust manual.toggle")
end