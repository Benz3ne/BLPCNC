local inst = mc.mcGetInstance()
if AuxLib and AuxLib.Request then
  -- vacFront and vacRear share #401 for AUTO; toggling either flips both.
  AuxLib.Request(inst, { device = "vacFront", action = "auto.toggle" })
else
  mc.mcCntlSetLastError(inst, "Missing AuxLib.Request: vac auto.toggle")
end

